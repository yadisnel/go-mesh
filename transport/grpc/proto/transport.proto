syntax = "proto3";
//go get github.com/golang/protobuf/protoc-gen-go@v1.3.2
package go.ms.transport.grpc;

service Transport {
	rpc Stream(stream Message) returns (stream Message) {}
}

message Message {
	map<string, string> header = 1;
	bytes body = 2;
}
